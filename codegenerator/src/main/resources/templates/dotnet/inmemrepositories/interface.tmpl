using {rootname}.Entities;
using System;
using System.Collections.Generic;

namespace {rootname}.Repositories
{
    public interface I{tablepascal}Repository
    {
        {tablepascal} SelectById(Guid {primarycolumncamel});

        {tablepascal} Insert({tablepascal} entity);

        IEnumerable<{tablepascal}> SelectAll();

        bool Update({tablepascal} entity);

        bool Delete(Guid {primarycolumncamel});{selectforparent}{selectbyunique}
    }
}

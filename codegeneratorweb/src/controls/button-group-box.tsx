import { Box, styled } from "@mui/system";

const ButtonGroupBox = styled(Box)(() => ({
  display: 'flex', 
  justifyContent: 'end', 
  marginTop: '2rem',
  marginBottom: '2rem'
}))

export default ButtonGroupBox;
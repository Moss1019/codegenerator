import { Paper, styled } from "@mui/material";

const StyledPaper = styled(Paper)(() => ({
  marginTop: '1.2rem',
  padding: '.5rem',
  marginRight: '.2rem',
  marginLeft: '.2rem',
  flexGrow: 1
}));

export default StyledPaper;

  public List<{tablepascal}> selectOf{parentpascal}({parentprimarydatatype} {parentprimarycamel}) {
    return collections.get(collectionName)
        .values()
        .stream()
        .map(x -> {
          {tablepascal} a = ({tablepascal})x;{setchildlists}
          return a;
        })
        .filter(x -> x.get{parentprimarypascal}().equals({parentprimarycamel}))
        .collect(Collectors.toList());
  }